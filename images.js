import preloader from 'spectacle/lib/utils/preloader';

const images = {
  background: require('assets/img/bg.png'),
  gif: require('assets/img/gql.gif'),
};

preloader(images);

export default images;
